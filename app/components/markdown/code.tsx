import { styled } from "react-tailwind-variants";

export const Code = styled("code", {
  base: "text-sm bg-surface0 rounded px-1 py-0.5",
})

export const Pre = styled("pre", {
  base: "text-sm bg-surface0 rounded-lg p-2",
})
